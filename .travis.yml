language: node_js
sudo: false
node_js:
  - 'lts/*'
  - 'node'

before_install:
  - npm i -g npm@latest

install:
  - npm ci
  - cd projects/ngx-esy-jsonapi && npm ci && cd ../..

script:
  - npm test -- --no-watch --no-progress --code-coverage --browsers=ChromeHeadlessCI
  - npm run lint
